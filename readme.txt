1. Unzip the folder
2. Open the file llm.py using a code editor
3. Install the neccessary libraries by typing in the command line-
    -pip install requirements.txt
4. Add the dataset to the project directory
5. Run the python file.